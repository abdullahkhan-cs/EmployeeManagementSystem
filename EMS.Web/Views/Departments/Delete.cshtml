@model EMS.Web.Models.ViewModels.DepartmentViewModel

@{
    ViewData["Title"] = "Delete Department";
}

<div class="card" style="max-width: 500px;">
    <div class="card-body p-4">
        <p>Are you sure you want to delete this department?</p>

        <dl class="row mb-0">
            <dt class="col-sm-4">Name</dt>
            <dd class="col-sm-8">@Model.Name</dd>

            <dt class="col-sm-4">Code</dt>
            <dd class="col-sm-8">@Model.Code</dd>

            <dt class="col-sm-4">Employees</dt>
            <dd class="col-sm-8">@Model.EmployeeCount</dd>
        </dl>

        @if (Model.EmployeeCount > 0)
        {
            <div class="alert alert-warning mt-3 mb-0">
                This department has employees assigned. Deletion will be blocked until they're reassigned.
            </div>
        }

        <form asp-action="Delete" method="post" class="mt-4">
            <input type="hidden" asp-for="Id" />
            <button type="submit" class="btn btn-danger">
                <i class="bi bi-trash me-1"></i> Delete
            </button>
            <a asp-action="Index" class="btn btn-outline-secondary">Cancel</a>
        </form>
    </div>
</div>